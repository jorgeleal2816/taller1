// Le pida al usuario un número y cree un arreglo de números empezando
//  en el 1 hasta el número que el usuario ingrese (incluyéndolo)
// Elimine el segundo elemento.
// Recorra e imprima el arreglo.
const numero = Number prompt( "ingrese un numero");
let numeros []
for(let i = 1; i <= numero; i++){
numero.push(i)
}

numero.splice(1.1);
for( let valor  of numeros );
console,log (valor);
